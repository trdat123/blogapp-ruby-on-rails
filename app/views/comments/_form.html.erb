<%= form_with(model: [@blog, @blog.comments.create]) do |f| %>
    <div class="form-group">
        <%= f.text_area :body, class:"form-control" %>
        <%= f.submit "Submit", class:"btn btn-success" %>
    </div>
<% end %>